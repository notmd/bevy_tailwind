[package]
name = "test_pass"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bevy = { version = "0.17.0-rc", default-features = false, features = [
    "bevy_ui",
    "bevy_text",
] }
bevy_tailwind_macro = { path = "../../" }
bevy_tailwind = { path = "../../../" }
